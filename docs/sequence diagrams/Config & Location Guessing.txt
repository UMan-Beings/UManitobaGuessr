title Game Configuration & Location Guessing
entryspacing 0.6
    participant Frontend
    participant Backend
    participant Database
    Frontend->Backend:POST /api/v1/games {totalRounds, maxTimerSeconds, userId}
    Backend->Database:Create game row
    Backend->Database:Create round rows with phases and location details
    loop Breaks on final round
Backend->Database:Request round with round number
Backend<-Database:Round info
alt First iteration
        Frontend<-Backend:Phase(GUESS), game ID, and initial game state (first location image, time limit, round#, etc.)
        else Later iterations
        
        Frontend<-Backend:Phase(GUESS) and game state for round (location image, time limit, round#, etc.)
end
        Frontend->Backend:POST /api/v1/games/{gameId}/guess {x, y, guessTimeSeconds}
        Backend->Database:Update round with new guess(x, y, and time)
        alt More rounds remaining
            Frontend<-Backend:Phase(REVEAL) and round details (score, true lat and long, guess time)
box over Backend:Increment round number
            Frontend->Backend:POST /api/v1/games/{gameId}/next
        else Final round
    Frontend<-Backend:Phase(FINISHED), and final cumulative score
    
        end
        
    end