title Game Configuration
entryspacing 0.6
    participant Frontend
    participant Backend
    participant Database
    Frontend->Backend:POST /api/v1/games {totalRounds, maxTimerSeconds, userId}
    Backend->Database:Create game row
    Backend->Database:Create round rows with phases and location details

Backend<-Database:Initial game info
        Frontend<-Backend:Game ID and initial game state (first location image, time limit, round#, etc.)
        else Later iterations